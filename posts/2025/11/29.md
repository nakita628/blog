---
date: 2025-11-29
title: TypeScript トップ型とボトム型
description: トップ型とボトム型
tags: 
    - TypeScript
prev:
    text: TypeScript サブタイプとスーパータイプ
    link: /posts/2025/11/28
next:
    text: TypeScript 複雑な型と互換性
    link: /posts/2025/11/30
---

# TypeScript トップ型とボトム型

&emsp;`TypeScript`の型システムにおいて、すべての型を包含する最も抽象的な型は`unknown`型。`unknown`型の変数にはどんな型の値でも代入可能。`unknown`型をすべての型のスーパータイプとして見ることで、その振る舞いを直観的に理解可能。

&emsp;逆に、すべての型のサブタイプとなるのは`never`型。空集合はすべての集合の部分集合であり、`never`型はこの空集合に相当する。つまり、`never`型には実際の値が存在せず、どんな値もこの型の変数に代入することはできない。`number`型と`string`型のインターセクション型が`never`型になるのも、`number`型でありながら`string`型でもある要素が存在しないため。

&emsp;型システムでは、すべての型を包含する`unknown`型を**トップ型**と呼び、すべての型に含まれる`never`型を**ボトム型**と呼ぶ。